/** Dimensiune maximă fișier import JSON (octeți). */
export const MAX_BACKUP_FILE_BYTES = 2 * 1024 * 1024

/** Lungime maximă câmp text în backup (caractere). */
export const MAX_SCRATCH_CHARS = 200_000
export const MAX_FOCUS_CHARS = 200

/** Limită superioară contor Pomodoro (anti-abuz localStorage). */
export const MAX_POMODORO_CYCLES = 1_000_000
